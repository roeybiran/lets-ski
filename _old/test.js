/* eslint-disable */

function setup() {
  createCanvas(500, 500);
  triangle(250, 500, 300, 500, 250, 250)
  fill('red')
  const magicX = 300
  const magicY = 500
  triangle(250, 400, magicX, magicY, 250, 250)
}
